# TCP Packages Overview

The `tcpclient` package provides a straightforward client implementation to demonstrate TCP connections with a server.

The `tcpcommon` package contains the core functionality required to establish and manage TCP connections.

The `tcpserver` package simulates a basic server that responds to client requests.
